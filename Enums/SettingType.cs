namespace Hifumi.Enums
{
    public enum SettingType
    {
        Prefix,
        JoinRole,
        MuteRole,
        ModChannel,
        JoinChannel,
        LeaveChannel,
        MaxWarnings,
        RedditChannel,
        StarboardChannel,
        LevelUpMessage,
        Locale,
        GuildActionChannel
    }
}
